"""Vendored third-party source.

Nothing in here is ours. See ``README.md`` in this directory for what is
vendored, why, and how to update it.

This file exists so the directory is a regular package rather than an
implicit namespace package: ``from third_party import pymysql`` should
mean the same thing however the interpreter is launched and whatever else
is on ``sys.path``.
"""
